# Archive

This directory contains retired implementations and historical documentation.
It is frozen for reference only and is excluded from the current application build and release pipeline.

The maintained Tauri desktop application lives in `app/`.
