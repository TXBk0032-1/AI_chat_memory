# cargo audit 配置。rsa 0.9.10 仅作为 sqlx 未激活可选驱动（sqlx-mysql）的
# lock 化石条目存在：Windows/Linux 构建产物均未编译 rsa（cargo tree --target all
# 与 target 产物核验确认），且上游对 RUSTSEC-2023-0071（Marvin Attack）暂无修复
# 版本（patched = []）。待 sqlx 精简默认特性或全量重锁清除化石后，可移除本忽略项。
[advisories]
ignore = ["RUSTSEC-2023-0071"]
